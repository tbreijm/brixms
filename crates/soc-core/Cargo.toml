[package]
name = "soc-core"
description = "SOC settlement-side engine core (ADR-0002): interning, persistent stores, chained history digest (E1); realization interface, Adm, and the naive reference oracle (S2⋈E2)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
brix-canon = { workspace = true }
brix-semantic = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
